_schema-version: "3.2"
ID: uploadexcel
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: uploadexcel-destination-content
  type: com.sap.application.content
  requires:
  - name: uploadexcel-destination-service
    parameters:
      content-target: true
  - name: uploadexcel-repo-host
    parameters:
      service-key:
        name: uploadexcel-repo-host-key
  - name: uploadexcel-uaa
    parameters:
      service-key:
        name: uploadexcel-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: uploadexcel_html_repo_host
          ServiceInstanceName: uploadexcel-html5-service
          ServiceKeyName: uploadexcel-repo-host-key
          sap.cloud.service: uploadexcel
        - Authentication: OAuth2UserTokenExchange
          Name: uploadexcel_uaa
          ServiceInstanceName: uploadexcel-xsuaa-service
          ServiceKeyName: uploadexcel-uaa-key
          sap.cloud.service: uploadexcel
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: uploadexcel-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: uploadexcel-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - uploadexcel.zip
      name: uploadexcel
      target-path: resources/
- name: uploadexcel
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: uploadexcel-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: uploadexcel-destination-service
    service-plan: lite
- name: uploadexcel-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: uploadexcel-xsuaa-service
    service-plan: application
- name: uploadexcel-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: uploadexcel-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
